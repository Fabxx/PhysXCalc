#include <libs.h>
//This functions frees all the allocated memory for the vectors to work with new values.
//erase deletes all the values, and we set the range, in this case, from beginning to the end.

void memory::dump() {
    
        angle::atanvett1.clear();
        angle::atanvett2.clear();
        single_module::modules1.clear();
        single_module::modules2.clear();
        single_module::X.clear();
        single_module::Y.clear();
        system("cls");
    
}