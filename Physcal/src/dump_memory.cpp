#include <libs.h>
//This functions frees all the allocated memory for the vectors to work with new values.

void memory::dump() {

    angle::atanvett.clear();
    single_module::moduleout.clear();
    system("cls");
}